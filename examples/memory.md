# Rulence project memory

Release freeze is active this week. Database migrations require rollback plans,
nightly backups, and explicit approval before production changes.

Rulence should ask memory before spending extra reasoning tokens.

Honcho stores internal user preferences. MemPalace stores external project
facts, notes, documents, and decisions.
